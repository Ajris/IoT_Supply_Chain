version: '3'
services:
  node_first:
    build:
          context: .
          args:
                name: "node1"
                rpcport: 8545
    ports:
      - 8545:8545
    networks:
            - private_network
  node_second:
    build: 
        context: .
        args:
                name: "node2"
                rpcport: 8456
    ports:
      - 8546:8546
    networks:
      - private_network


networks:
        private_network:
                external:
                        name: private_network

    



